name: Add recurring weekday events to notion

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Use Node.js
        env:
          NOTION_KEY: ${{deploy.secrets.NOTION_KEY}}
          NOTION_DATABASE_ID: ${{deploy.secrets.NOTION_DATABASE_ID}}
        uses: actions/setup-node@v3
        with:
          node-version: "12.x"
      - run: npm ci
      - run: npm run build --if-present
      - run: npm run weekdays
